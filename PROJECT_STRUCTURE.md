# Project Structure

```
wildfire-decision-support/
│
├── .env                          # Environment variables (local, not committed)
├── .env.example                  # Template for .env
├── requirements.txt              # Python dependencies
├── PROJECT_STRUCTURE.md
│
├── backend/                      # Flask application
│   ├── main.py                   # Entry point: build_env() → create_app() → Flask
│   ├── config.py                 # Loads .env variables
│   │
│   ├── api/                      # API route blueprints
│   │   ├── auth.py               # POST /api/auth/register, login, verify
│   │   ├── events.py             # GET  /api/events, /api/events/:id
│   │   └── timesteps.py          # All per-timestep endpoints (see API Routes below)
│   │
│   ├── pipeline/                 # Full startup orchestrator (runs before Flask)
│   │   ├── __init__.py           # build_env(): db → env → build_playback_events → checks
│   │   ├── db/
│   │   │   └── __init__.py       # setup_db(app): ensure_db + migrate + create_all + seed_db
│   │   ├── env.py                # prepare_all_events(): models + ERA5 + FIRMS + fire_state + landmarks
│   │   ├── check/
│   │   │   ├── __init__.py       # run_checks(): verify models, static gpkg, per-event files
│   │   │   └── builder.py        # build_playback_events(): slot grid → prediction + spatial stages
│   │   ├── predict/
│   │   │   ├── __init__.py
│   │   │   ├── prediction.py     # Stage 1: ML inference → GeoJSON files + fire_context.json
│   │   │   └── risk_zones.py     # build_risk_geojson(), load_youden_threshold()
│   │   └── spatial/
│   │       ├── __init__.py
│   │       └── spatial.py        # Stage 2: roads + population overlay → roads.geojson + DB counts
│   │
│   ├── agents/                   # On-demand AI analysis (triggered by POST /report)
│   │   ├── __init__.py
│   │   ├── _client.py            # call_llm() / stream_llm() — Claude or Gemini
│   │   ├── prompts.py            # System prompts for all agents
│   │   ├── risk_agent.py         # fire_context → risk analysis text
│   │   ├── impact_agent.py       # fire_context + population → impact text
│   │   ├── evacuation_agent.py   # fire_context (road_summary) → evacuation text
│   │   ├── summary_agent.py      # 3 agent outputs → executive briefing
│   │   └── chat_agent.py         # Stateless streaming chat
│   │
│   ├── db/
│   │   ├── connection.py         # db, get_db_uri(), ensure_db(), seed_db()
│   │   └── models.py             # ORM: User, FireEvent, EventTimestep
│   │
│   └── utils/
│       └── auth_middleware.py    # JWT verification middleware
│
├── frontend/                     # Static frontend (served by Flask)
│   ├── home.htm
│   ├── login.htm
│   ├── css/
│   └── js/
│       ├── auth.js
│       ├── map.js
│       ├── wildfire.js
│       ├── risk_zones.js
│       ├── controls.js           # Replay scrubber (3h slot timeline)
│       └── assistant.js          # Chat UI + report trigger
│
├── data/
│   ├── events/                   # Per-event data (generated by pipeline)
│   │   └── 2016_0001/            # {year}_{event_id:04d}
│   │       ├── landmarks.json    # Named places near event bbox (Overpass / Nominatim)
│   │       ├── data_processed/
│   │       │   ├── grid_static.parquet       # 500 m grid: dtm/slope/aspect/fuel_type per cell
│   │       │   ├── weather/
│   │       │   │   ├── era5.parquet          # hourly ERA5-Land (temp, wind, RH, precip)
│   │       │   │   ├── ffmc_daily.parquet    # daily FFMC (Fine Fuel Moisture Code)
│   │       │   │   ├── isi_hourly.parquet    # hourly ISI (Initial Spread Index)
│   │       │   │   └── ros_hourly.parquet    # hourly ROS (Rate of Spread)
│   │       │   ├── landcover/
│   │       │   │   └── fuel_type.tif         # reprojected FBP fuel type raster (EPSG:3978)
│   │       │   ├── firms/
│   │       │   │   └── hotspots.parquet      # preprocessed FIRMS satellite hotspots
│   │       │   └── training/
│   │       │       └── fire_state.pkl        # fire perimeters + overpass timeline
│   │       └── timesteps/        # One folder per canonical 3h slot
│   │           └── 2016-05-04T0600/
│   │               ├── prediction/
│   │               │   ├── perimeter.geojson
│   │               │   ├── hotspots.geojson
│   │               │   ├── risk_zones_3h.geojson
│   │               │   ├── risk_zones_6h.geojson
│   │               │   ├── risk_zones_12h.geojson
│   │               │   └── fire_context.json     # fire metrics, weather, FWI, wind forecast, road_summary
│   │               └── spatial_analysis/
│   │                   ├── roads.geojson         # major roads: status + cut_at + cut_location
│   │                   └── ai_summary.json       # on-demand: risk, impact, evacuation, situation_overview
│   │
│   └── static/                   # Shared datasets (auto-downloaded at startup from Zenodo)
│       ├── models/               # ML models (Zenodo: records/19435138)
│       │   ├── model_full_rf.pkl
│       │   ├── model_full_xgb.pkl
│       │   ├── model_full_lr.pkl
│       │   └── model_full_thresholds.json
│       ├── population.gpkg       # Dissemination areas + census population (Zenodo: records/19434352)
│       └── roads_canada.gpkg     # OSM Canada major roads (Zenodo: records/19436338)
│
├── docker/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── init.sql
│   ├── DOCKER_SETUP.md
│   └── NON_DOCKER_SETUP.md
│
└── docs/
    └── api.yaml                  # OpenAPI 3.0 spec
```

## API Routes

| Method | Path | Description |
|--------|------|-------------|
| POST | `/api/auth/register` | Register |
| POST | `/api/auth/login` | Login → JWT |
| GET  | `/api/auth/verify` | Verify token |
| GET  | `/api/events` | List all fire events |
| GET  | `/api/events/:id` | Event detail + bbox |
| GET  | `/api/events/:id/timesteps` | List 3h slots (replay scrubber) |
| GET  | `/api/events/:id/timesteps/:ts_id/perimeter` | Fire perimeter GeoJSON |
| GET  | `/api/events/:id/timesteps/:ts_id/hotspots` | Satellite hotspots GeoJSON |
| GET  | `/api/events/:id/timesteps/:ts_id/risk-zones` | Risk zones (3h/6h/12h combined) |
| GET  | `/api/events/:id/timesteps/:ts_id/roads` | Major roads with status + cut locations GeoJSON |
| GET  | `/api/events/:id/timesteps/:ts_id/analysis` | Population counts (from DB) |
| GET  | `/api/events/:id/timesteps/:ts_id/fire-context` | Fire metrics, weather, FWI, wind forecast, road summary |
| POST | `/api/events/:id/timesteps/:ts_id/report` | Generate AI situation report (cached) |
| POST | `/api/events/:id/chat` | Stateless streaming chat (`{message, timestep_id, history[]}`) |

## DB Schema

```
fire_events        → id, name, year, bbox (POLYGON/4326), time_start, time_end,
                     description, end_date (NULL = realtime, value = playback)

event_timesteps    → id, event_id (FK),
                     slot_time        ← canonical 3h grid timestamp
                     nearest_t1       ← most recent satellite overpass ≤ slot_time
                     gap_hours        ← slot_time − nearest_t1 [h] (always ≥ 0)
                     data_gap_warn    ← True if gap_hours > 12
                     prediction_status, spatial_analysis_status  ('pending'|'running'|'done'|'failed')
                     affected_population, at_risk_3h, at_risk_6h, at_risk_12h

users              → id, username, password, created_at
```

## Pipeline Flow

```
python main.py
  └─ build_env()
       │
       ├─ pipeline/db/setup_db(app)
       │    ├─ ensure_db()           ← create postgres DB + PostGIS extension
       │    ├─ _migrate_event_timesteps()  ← drop table if schema is stale (safe: fully regenerated)
       │    ├─ db.create_all()       ← create/update tables from ORM
       │    └─ seed_db()             ← insert FireEvent rows if table is empty
       │
       ├─ pipeline/env/prepare_all_events(app)
       │    └─ per FireEvent:
       │         ├─ _fetch_landmarks()              ← Overpass / Nominatim → landmarks.json
       │         ├─ whp.ensure_era5_coverage()      ← download + preprocess ERA5 if needed
       │         ├─ whp.collect_environment([landcover])   ← download fuel_type.tif if needed
       │         ├─ whp.preprocess_environment([landcover]) ← reproject to EPSG:3978
       │         ├─ whp.build_fire_weather_index()  ← ffmc_daily / isi_hourly / ros_hourly
       │         ├─ whp.build_grid()                ← grid_static.parquet
       │         ├─ whp.collect_hotspots()          ← FIRMS satellite data
       │         ├─ whp.preprocess_hotspots()
       │         └─ build_fire_state() → fire_state.pkl
       │
       ├─ pipeline/check/builder.build_playback_events()  (FireEvent.end_date IS NOT NULL)
       │    └─ per playback event:
       │         ├─ load Study + fire_state + RF predictor
       │         ├─ generate 3h slot grid (time_start → time_end)
       │         ├─ upsert EventTimestep rows (slot_time, nearest_t1, gap_hours)
       │         └─ per timestep (skip stage if status == 'done'):
       │              │
       │              ├─ Stage 1 · predict/prediction.py
       │              │    ├─ run RF prediction × [3h, 6h, 12h]
       │              │    ├─ → risk_zones_{3,6,12}h.geojson
       │              │    ├─ → perimeter.geojson
       │              │    ├─ → hotspots.geojson
       │              │    └─ → fire_context.json  (fire metrics, weather, FWI, wind forecast)
       │              │
       │              └─ Stage 2 · spatial/spatial.py
       │                   ├─ roads_canada.gpkg × perimeter/risk zones → roads.geojson
       │                   │    (status: burned/at_risk_Xh/clear, cut_at, cut_location)
       │                   ├─ road_summary → merged into fire_context.json
       │                   └─ population.gpkg × perimeter/risk zones → DB counts
       │                        (affected_population, at_risk_3h/6h/12h)
       │
       └─ pipeline/check/run_checks(app)
            ├─ verify ML models (data/static/models/)
            ├─ verify static GeoPackages (population.gpkg, roads_canada.gpkg)
            └─ verify per-event files (era5.parquet, fire_state.pkl)

─────────────────────────────────────────────────────────────────
On-demand (user clicks "Generate Report"):
  POST /api/events/:id/timesteps/:ts_id/report
    ├─ return cached ai_summary.json if exists
    ├─ load fire_context.json (includes road_summary)
    ├─ read population counts from DB
    ├─ risk_agent(fire_context) → risk analysis
    ├─ impact_agent(fire_context, population) → impact analysis
    ├─ evacuation_agent(fire_context) → evacuation analysis
    ├─ summary_agent(risk, impact, evacuation) → situation overview
    └─ write ai_summary.json → return JSON

Realtime pipeline: TODO (FireEvent.end_date IS NULL)
```

## Development Workflow

Each development cycle follows these steps:

1. **Read PROJECT_STRUCTURE.md** — sync with current architecture, pending tasks, and schema
2. **Implement** — make code changes to backend/frontend/pipeline
3. **Test** — start Flask server and verify endpoints manually or via scripts
4. **Commit** (no push) — `git add <files> && git commit -m "..."` (one-line message, no co-author trailer)
5. **Repeat from step 1**

### Pending Implementation Tasks

| # | Task | Files | Status |
|---|------|-------|--------|
| 1 | `cut_location` → array in `spatial.py` + `_road_summary` | `pipeline/spatial/spatial.py` | ✅ |
| 2 | Chat context: include `road_summary` from fire_context | `api/timesteps.py`, `agents/chat_agent.py` | ✅ |
| 3 | Chat prompt: suggest 3 follow-up questions | `agents/prompts.py` | ✅ |
| 4 | LLM error handling: stream error to client / 502 on report | `agents/chat_agent.py`, `api/timesteps.py` | ✅ |
| 5 | Gemini via REST API (`_client.py`); no double `build_env` | `agents/_client.py`, `main.py` | ✅ |
| 6 | Evacuation agent prompt updated for `cut_location` list | `agents/prompts.py` | ✅ |

All backend API endpoints are implemented and smoke-tested. No pending backend tasks.

---

## Static Data Sources

| File | Zenodo Record | Contents |
|------|--------------|----------|
| `data/static/models/` | [19435138](https://zenodo.org/records/19435138) | Trained RF/XGBoost/LR models + thresholds |
| `data/static/population.gpkg` | [19434352](https://zenodo.org/records/19434352) | Dissemination areas + census population 2011/2016/2021 |
| `data/static/roads_canada.gpkg` | [19436338](https://zenodo.org/records/19436338) | OSM Canada major roads |
